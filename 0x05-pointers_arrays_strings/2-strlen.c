#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the legth of a string
 *
 * @s: The string to be checked
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
