#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _atoi - function that converts a string to an integer
 *
 * @s: The string to be converted
 */
int _atoi(char *s)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf("%d", a[i]);
		if (i != n - 1)
		{
			printf(", ");
		}
	}
	printf("\n");
}
